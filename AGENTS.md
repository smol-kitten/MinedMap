# AGENTS.md — MinedMap

<!-- agentsmd:generated -->
> Auto-generated by `agentsmd.py` (deterministic, no AI) · 2026-07-30 · chore/agents-md-entry@9d3e198.
> This is the canonical entry file for **any** coding agent. Edit only inside the custom block below — the rest is regenerated.

## What this is

* Render beautiful maps of your [Minecraft](https://minecraft.net/) worlds! * Put them on a webserver and view them in your browser! * Compatible with unmodified Minecraft Java Edition 1.8 up to 26.1 (no mod installation required!) * Illumination layer: the world at night * Fast: create a full map for a huge 3GB savegame in less than 5 minutes in single-threaded operation * Multi-threading support: pass `-j N` to the renderer to use `N` parallel threads for generation * Incremental updates: only recreate map tiles for regions that have changed * Typically uses less than 100MB of RAM in single-threaded operation (may be higher when `-j` is passed) * Cross-platform: runs on Linux, Windows, and likely other systems like MacOS as well

## Build / test / run

```bash
cargo build                             # Build
cargo test                              # Test
```

## Where to look (machine discovery)

These are kept fresh automatically — read them before grepping:

- `docs/` — human design docs

## Contributing conventions

- Branch → PR → merge. Never commit directly to the default branch, never force-push, never `--admin`-merge.
- Keep this file's generated sections in sync by rerunning `agentsmd.py gen`; put durable hand-written notes in the custom block.

<!-- agentsmd:custom:start -->
<!-- Hand-written notes below are preserved across regeneration. -->

<!-- agentsmd:custom:end -->
